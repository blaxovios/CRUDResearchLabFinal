@model IEnumerable<CRUDResearchLabFinal.Models.PublicationMedia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PublicationType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PublicationType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PublicationMediaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PublicationMediaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PublicationMediaID })
        </td>
    </tr>
}

</table>
