@model IEnumerable<CRUDResearchLabFinal.Models.Publication>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ThematicUnit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PublicationYear)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LabAnnouncement.Announcement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LabMember.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PublicationMedia.PublicationType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ResearchProject.Running)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ThematicUnit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PublicationYear)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LabAnnouncement.Announcement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LabMember.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PublicationMedia.PublicationType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ResearchProject.Running)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PublicationsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PublicationsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PublicationsID })
        </td>
    </tr>
}

</table>
